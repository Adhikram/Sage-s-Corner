<% provide(:title, 'All users') %> 
<h1>All users</h1> 
<div class="search_form">
<%= form_tag("/users", :method => "get", class: 'd-flex') do %>
   <%= text_field_tag(:search) %>
   <%= submit_tag("Search") %>
<% end %>
</div>

<ul class="users"> 
	<%= render @users %> 
</ul>
